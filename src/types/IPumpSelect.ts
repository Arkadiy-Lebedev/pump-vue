export interface IPumpSelect {
    image: string,
    type: string,
    pumpX: number | null,
    pumpY: number | null,
}

export interface IworkPointSelect {  
    pumpX: number | null,
    pumpY: number | null,
}